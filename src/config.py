n_centers_generated = 40
n_samples = 2000
n_samples_per_client = 10
n_features = 2
random_state = 42
merging_threshold = 4.0
visualize = False
verbose = False
n_iterations = 100
max_iterations_clustering = 1
# n_clients = 6
# n_clusters = [6, 7, 8, None, None, None]
# clustering_methods = ["kmeans", "kmeans", "kmeans", "meanshift", "meanshift", "meanshift"]
# n_clients = 3
# n_clusters = [None, 3, 3]
# clustering_methods = ["meanshift", "kmeans", "kmeans"]
# n_clients = 5
# n_clusters = [3, 3, 3, 3, 3]
# clustering_methods = ["kmeans", "kmeans", "kmeans", "kmeans", "kmeans"]
n_clients = 12
# clustering_methods = ["meanshift"] * (n_clients // 2) + ["kmeans"] * (n_clients // 2)
# n_clusters = [None] * (n_clients // 2) + [8] * (n_clients // 2)
# clustering_methods = ["kmeans"] * (n_clients // 4) + ["mini_batch_kmeans"] * (n_clients // 4) + ["meanshift"] * (n_clients // 4) + ["kmedoids"] * (n_clients // 4)
# n_clusters = [8] * (n_clients // 4) + [8] * (n_clients // 4) + [None] * (n_clients // 4) + [8] * (n_clients // 4)
clustering_methods = ["kmeans"] * (n_clients // 3) + ["mini_batch_kmeans"] * (n_clients // 3) + ["meanshift"] * (n_clients // 3)
n_clusters = [8] * (n_clients // 3) + [8] * (n_clients // 3) + [None] * (n_clients // 3)